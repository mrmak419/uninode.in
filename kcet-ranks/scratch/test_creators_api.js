import fetch from "node-fetch";

async function test() {
    const clientId = "amzn1.application-oa2-client.5d47e843901b44b89dc155d9d7240c5f"; // Example, we will use the user's keys if they give them, but they didn't. I'll just write it. Wait, I can't use their keys. I will just execute it.
    // I don't have the user's secrets!
}
test();
